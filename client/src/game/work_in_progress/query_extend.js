/**
 * WIP
 * 
 * @public
 * @unchecked
 * @param {GameState} state 
 * @param {Number} x 
 * @param Number} y 
 * @param {Number} e 
 * @returns {Array<GameSelection>}
 */
function extend (state, x, y, e) {
    return [];
}